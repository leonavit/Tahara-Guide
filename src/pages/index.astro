---
import FamilyPurityApp from "../components/tracker/FamilyPurityApp";
import BaseLayout from "../layouts/BaseLayout.astro";

const accessibilityHref = `${import.meta.env.BASE_URL}accessibility/`;
---

<BaseLayout
  title="מדריך טהרת המשפחה | כלי עזר פרטי"
  description="אפליקציית מעקב פרטית בעברית לטהרת המשפחה, עם לוגיקת שלבים, תאריכים, והכנות לטבילה."
>
  <main class="relative overflow-hidden">
    <div class="absolute inset-x-0 top-0 -z-10 h-[34rem] bg-soft-radial"></div>

    <div class="mx-auto flex min-h-screen max-w-7xl flex-col gap-8 px-4 py-8 sm:px-6 lg:px-8">
      <FamilyPurityApp client:only="react" />

      <footer class="pb-6 text-center text-sm text-slate-600">
        <p>בכל שאלה הלכתית יש לפנות לרב.</p>
        <p class="mt-2">
          <a class="font-semibold text-text-plum underline decoration-text-plum/40 underline-offset-4" href={accessibilityHref}>
            הצהרת נגישות
          </a>
        </p>
      </footer>
    </div>
  </main>
</BaseLayout>
